spring:
  kafka:
    consumer:
#      bootstrap-servers: ${INPUT_KAFKA_BOOTSTRAP_SERVER}
#      group-id: ${CONSUMER_GROUP_ID}
#      topics-title: ${CONSUMER_TOPIC_NAME}
      bootstrap-servers: localhost:9092
      group-id: group
      topics-title: topic
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: none
